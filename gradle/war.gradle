apply plugin: "war"

bootWar {
    mainClassName = "com.spingular.chat.SpingularchatApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
